-- This script creates a GUI with the rolled rarity displayed in the middle of the screen, and options to equip and delete on the side of the rolled rarity, with the roll button at the bottom. It also displays the equipped rarity above the player's head.

local player = game.Players.LocalPlayer
local gui = Instance.new("ScreenGui")
gui.IgnoreGuiInset = true
gui.Parent = player.PlayerGui

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 200, 0, 200)
frame.Position = UDim2.new(0.5, -100, 0.5, -100)
frame.BackgroundTransparency = 1
frame.BorderSizePixel = 0
frame.Parent = gui

local background = Instance.new("Frame")
background.Size = UDim2.new(1, 0, 1, 0)
background.BackgroundColor3 = Color3.new(0, 0, 0)
background.BackgroundTransparency = 0.5
background.Parent = frame

local rarityText = Instance.new("TextLabel")
rarityText.Size = UDim2.new(0.6, 0, 0, 50)
rarityText.Position = UDim2.new(0.05, 0, 0.2, 0)
rarityText.BackgroundColor3 = Color3.new(1, 1, 1)
rarityText.BorderSizePixel = 0
rarityText.Text = ""
rarityText.Parent = frame

local equipButton = Instance.new("TextButton")
equipButton.Size = UDim2.new(0.3, 0, 0, 50)
equipButton.Position = UDim2.new(0.05, 0, 0.7, 0)
equipButton.BackgroundColor3 = Color3.new(0, 0.5, 0.5)
equipButton.BorderSizePixel = 0
equipButton.Text = "Equip"
equipButton.Visible = false
equipButton.Parent = frame

local deleteButton = Instance.new("TextButton")
deleteButton.Size = UDim2.new(0.3, 0, 0, 50)
deleteButton.Position = UDim2.new(0.35, 0, 0.7, 0)
deleteButton.BackgroundColor3 = Color3.new(0.5, 0, 0)
deleteButton.BorderSizePixel = 0
deleteButton.Text = "Delete"
deleteButton.Visible = false
deleteButton.Parent = frame

local rollButton = Instance.new("TextButton")
rollButton.Size = UDim2.new(0.6, 0, 0, 50)
rollButton.Position = UDim2.new(0.05, 0, 0.9, 0)
rollButton.BackgroundColor3 = Color3.new(0.2, 0.2, 0.2)
rollButton.BorderSizePixel = 0
rollButton.Text = "Roll"
rollButton.TextColor3 = Color3.new(1, 1, 1)
rollButton.FontSize = Enum.FontSize.Size24
rollButton.Parent = frame

local equippedRarityText = Instance.new("TextLabel")
equippedRarityText.Size = UDim2.new(0, 100, 0, 20)
equippedRarityText.Position = UDim2.new(0.5, -50, 0, 0)
equippedRarityText.BackgroundColor3 = Color3.new(1, 1, 1)
equippedRarityText.BorderSizePixel = 0
equippedRarityText.Text = ""
equippedRarityText.Visible = false
equippedRarityText.Parent = player.PlayerGui

local rarityNames = {
    [1] = "Diamond",
    [2] = "Emerald",
    [3] = "Ruby",
    [4] = "Sapphire",
    [5] = "Amethyst",
    -- Add more names for additional rarities
}

local rarityEffects = {
    [1] = {AuraColor = Color3.fromRGB(255, 0, 0), Effect = "Fire"},
    [2] = {AuraColor = Color3.fromRGB(0, 255, 0), Effect = "Sparkles"},
    [3] = {AuraColor = Color3.fromRGB(0, 0, 255), Effect = "Smoke"},
    [4] = {AuraColor = Color3.fromRGB(255, 255, 0), Effect = "Neon"},
    [5] = {AuraColor = Color3.fromRGB(255, 0, 255), Effect = "Glow"},
    -- Add more rarities and effects as needed
}

local function applyRarityEffects(rarity)
    local aura = Instance.new("ParticleEmitter")
    aura.Parent = player.Character.HumanoidRootPart
    aura.Color = rarityEffects[rarity].AuraColor
    aura.Texture = "http://www.roblox.com/asset/?id=546494090"
    aura.Size = NumberSequence.new(2)
    aura.Lifetime = NumberRange.new(5)
    aura.Rate = 5
    aura.Speed = NumberRange.new(10)
    
    local effect = Instance.new("Smoke")
    effect.Parent = player.Character.HumanoidRootPart
    effect.Color = rarityEffects[rarity].AuraColor
    effect.Size = 10
    effect.RiseVelocity = 5
    
    wait(5) -- Duration of the effect
    aura:Destroy()
    effect:Destroy()
end

local function applySpecialEffect()
    -- Add your special effect here
    print("Applying special effect for rare rarity!")
    -- For example, you can create a burst of particles or play a sound
end

local function showRarityText(rarity)
    rarityText.Text = "Rarity: " .. rarityNames[rarity]
end

local function clearRarityText()
    rarityText.Text = ""
end

rollButton.MouseButton1Click:Connect(function()
    local rarity = math.random(1, 5) -- Replace this with your rarity roll logic
    print("Rolled rarity: " .. rarity)
    
    showRarityText(rarity)
    
    if rarity <= 3 then
        equipButton.Visible = true
        deleteButton.Visible = true
    else
        equipButton.Visible = true
        deleteButton.Visible = true
        applySpecialEffect()  -- Apply special effect for rare rarity
    end
end)

equipButton
.MouseButton1Click:Connect(function() local rarity = math.random(1, 5) -- Replace this with the rarity of the equipped item print("Equipping item with rarity: " .. rarity) applyRarityEffects(rarity)clearRarityText()
equipButton.Visible = false
deleteButton.Visible = false

equippedRarityText.Text = rarityNames[rarity]
equippedRarityText.Visible = true
wait(5)  -- Duration for the equipped rarity to be displayed
equippedRarityText.Visible = false
end)deleteButton.MouseButton1Click:Connect(function() print("Deleting item") clearRarityText() equipButton.Visible = false deleteButton.Visible = false end)
This script now arranges the buttons and text elements as per your instructions, and it displays the equipped rarity above the player's head when equipped. Adjust as needed for your game.
